# Optional: enables LLM-based clinical note summarization (Tab 3).
# Without this set, the app falls back to a transparent rule-based summary --
# no functionality is lost, per README.
ANTHROPIC_API_KEY=
